import es.tmoor.scanvas._

import Bounds._
import shapes.RoundedRect

object MenuBar extends Template {
  def relativeBoundingBox: Bounds = (0,0,1,1)
  def render(context: Context, renderer: Renderer): Unit = {

  }
  
}